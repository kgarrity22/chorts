import { Box, Typography } from "@mui/material";

// TODO: image grid of all the visuals to demo
export const Home = () => {
  return (
    <Box>
      <Typography variant="h1">A series of visualization projects</Typography>
    </Box>
  );
};
